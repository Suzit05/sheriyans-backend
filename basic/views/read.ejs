<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-800 text-white ">
    <h1 class="text-xl self-center border-b mt-4">the User</h1>
    <div class="m-4">
        <a class="text-blue-500" href="/">back</a>
    </div>
    <div class="w-full h-full flex flex-wrap justify-center items-center gap-3">
        <% if (users && users.length> 0) { %>
            <% users.forEach(function(user) { %>
                <div class=" bg-zinc-700 border-black w-fit p-4 m-4 rounded-lg">
                    <div class="bg-zinc-500 p-2 w-[12vw] h-[30vh] rounded-md shadow">
                        <img src="<%= user.imageUrl %>" alt="">
                    </div>
                    <h1>
                        <%= user.name %>
                    </h1>
                    <h3>
                        <%= user.email %>
                    </h3>
                    <div class="flex gap-3">
                        <a class="text-blue-500" href="">Edit user</a>
                        <a class="text-red-500" href="">Delete user</a>
                    </div>
                </div>
                <% }); %>
                    <% } else { %>
                        <p>No users found!</p>
                        <% } %>




    </div>
</body>

</html>